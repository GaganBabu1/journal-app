version: "3.9"

services:
  journal-backend:
    build:
      context: ./journalApp
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    restart: always
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 700m